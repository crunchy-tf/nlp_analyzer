fastapi>=0.100.0
uvicorn[standard]>=0.22.0
loguru>=0.7.0
pydantic>=1.10.0

# --- Core dependencies for BERTopic/Sentiment - PIN THESE TO YOUR TRAINING ENV VERSIONS ---
# Replace X.Y.Z with the versions from your `pip freeze` on the training VM
bertopic==0.16.0          # <<<--- EXAMPLE! FIND YOUR EXACT VERSION
scikit-learn==1.3.2       # <<<--- EXAMPLE! FIND YOUR EXACT VERSION
pandas==2.0.3             # <<<--- EXAMPLE! FIND YOUR EXACT VERSION
numpy==1.26.0             # <<<--- EXAMPLE! FIND YOUR EXACT VERSION
umap-learn==0.5.4         # <<<--- EXAMPLE! FIND YOUR EXACT VERSION
hdbscan==0.8.33           # <<<--- EXAMPLE! FIND YOUR EXACT VERSION
# sentence-transformers might be okay with >= but if BERTopic pinned it, match that too.
sentence-transformers>=2.2.0 
# joblib might be installed as a dependency of scikit-learn or hdbscan
# joblib==A.B.C # If you find a specific version was used

# --- Dependencies for Transformers/Sentiment ---
transformers[torch]>=4.30.0 # Or pin if needed: transformers[torch]==4.35.0
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
sentencepiece>=0.1.98     # <<<--- ADDED/UPDATED
protobuf>=3.20.0          # <<<--- ADDED/UPDATED

# --- Other dependencies ---
yake>=0.4.8
apscheduler>=3.10.0